# tuples is type of structure, like a container
# immutable, couldn't be changed the elements inside

coordinates = [(4, 5), (3, 78), (98, 12)]
# coordinates[0] = 10 || Error!! Cause we can't change the value inside
print(coordinates[0:])
